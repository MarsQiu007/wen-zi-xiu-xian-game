# Problems

## 2026-04-19 (Task 11 行为链移动接线)
- NPC 行为链的“哪类 intent 视为 travel”目前仍是启发式映射（`seek_support/secure_resources/raise_reputation`），后续若补充更细粒度动作语义，需要同步更新映射表以避免误判。

## 2026-04-19 (Task 11 修复复验)
- 当前 smoke 套件里缺少“专门断言 NPC 移动 from/to/cause 字段”的独立脚本，导致 NPC 移动验证主要靠日志回读与人工检查；后续可补一个 task11 专用 smoke 脚本（非本次范围）。

## 2026-04-19 (LocationService 地图崩溃修复)
- `get_all_regions()` 当前仍直接耦合 `WorldRegionData` 属性名（id/display_name/summary 等），若后续区域资源字段重命名，需要同步更新该映射层，否则地图会出现空字段但不一定抛错。

## 2026-04-19 (task10 地图烟测去脆弱化)
- 当前 map smoke 仍属于 UI 交互级别验证，尚未拆成纯数据层单测；若后续要进一步提速 CI，可再拆一层仅验证 `LocationService.get_all_regions()` 结果基线（非本次范围）。

## 2026-04-19 (T5 事件频控阈值收口)
- 当前 `event_diversity` 的窗口频控校验为 O(n²) 遍历，200 tick 下可接受；若后续将回归 tick 提升到数千级，建议改为滑窗计数器（非本次范围）。

## 2026-04-19
- 如果继续沿用当前 boot 方式，玩家会在没有选择前就进入模拟，导致 New/Continue/Mode 无法成立。
- 需要先找到最小改动点，避免把菜单逻辑散落到多个脚本里。

## 2026-04-19 (Update)
- 继续游戏的语义和加载机制目前仍然缺失（在 UI 层面被禁用），需要后续存档系统支持。

## 2026-04-19 (Save Protocol Task 2)
- 当前仅完成存储协议层（slot/version/atomic-write/校验与错误状态），尚未接入 Continue 的实际流程。
- 现阶段默认使用 `default` 槽位兼容旧 API；多槽位选择和 UI 绑定仍是后续任务。

## 2026-04-19 (Simulation Snapshot Task 3)
- 当前快照恢复仅覆盖 Continue 最小必需字段与日志游标一致性校验，尚未纳入 `_human_runtime/_deity_runtime/_pending_checkpoint` 的完整恢复；这是有意的最小范围限制，后续 Continue 集成时需扩展。

## 2026-04-19 (Task 4 New/Continue 语义)
- Continue 的快照来源目前默认依赖 `SaveService.load_game()` 返回的 `data` 结构中存在 `simulation_snapshot`；若后续存档聚合结构调整，需要同步更新该读取键名。
- 当前已支持“快照根字典”兼容分支，但正式协议仍建议统一使用 `simulation_snapshot` 包装字段，避免语义歧义。

## 2026-04-19 (Task 5 事件去重与频控)
- 在当前最小样本池（2 个普通模板）下，200 tick 全局单模板占比 ≤35% 在数学上不可达；系统会收敛到近似 50/50。若要满足 35%，需要后续扩充普通模板池（非本任务范围）。
- 当前已在不重构系统前提下显著降低 aftermath 的 festival 连刷，并保证普通模板在 10 tick 同 stage 窗口内不超过 2 次。

## 2026-04-19 (Task 6 UI Log Presentation)
- 日志聚合目前只能聚合“连续出现的相同事件”。如果在两个相同事件中间插了一个系统日志或其他角色的动作，聚合会被打断。后续可能需要时间窗聚合或更智能的分组合并逻辑。
- `EventLog` 的限制上限仍是 `MAX_ENTRIES = 200`，长时游玩下如果不通过持久化数据库进行日志翻页，过往信息会被裁切。


## 2026-04-19 (Task 12 解析错误修复后遗留)
- e2e 场景仍未满足 AC04/AC07/AC08（主要是菜单可见性与地图/角色面板链路断言），需要后续在不放宽验收标准前提下继续收口。
- 当前任务只处理“解析错误修复并恢复可执行链路”，未扩展到重新设计 e2e 场景构造方式。


## 2026-04-19 (Task 12 收口后状态)
- 当前 T12 e2e/failure_paths 已通过，暂未发现新的阻塞级未决问题。


## 2026-04-19 (Final Wave F4 修复：证据补齐与范围收敛)
- 当前工作树仍包含其它并行计划/模块改动（非 task13/14/15），本次只按指令完成“task13/14/15 范围外产物清理”；若后续 F4 需“仅保留本计划全部相关改动”，仍需额外做跨计划改动隔离（非本次范围）。


## 2026-04-19 (F4 范围收敛第二轮清理)
- 当前仍有少量工具/环境态文件改动（如 `.gitignore`、`.sisyphus/boulder.json`）是否纳入“计划相关”边界存在解释空间；本轮未做破坏性处理，建议在最终F4时按评审口径决定是否继续收缩。
